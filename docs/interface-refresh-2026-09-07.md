# Interface refresh, 7 September 2026

Vlak now has 24 working interface studies and 52 specialist component additions, taking the public component catalogue from 114 to 166. The studies connect those controls into useful local workflows across health, civic services, science, industrial tools, creative software, robotics, circuitry and microbiology. Genomics remains part of Science. Component APIs, registry metadata, source closure, examples and generated outputs are described in [the component checkpoint](specialist-workbenches-2026-09-07.md).

## Shared interface language

The application studies use paper and ink tokens, Inter, hairlines, flush structural dividers and Vlak controls. Content receives padding inside each region; its divider meets the region's edges. Specialized plots, molecular illustrations, routing matrices, phone forms and Three.js scenes retain the visual structure their tasks need.

The shared shell provides the catalogue, responsive contents picker, source and component links, an interaction summary, and a copyable Build with Vlak brief. Gallery crops show real fragments of each interface, with text kept inside word boundaries. The first eight specialist crops now extend their title-bar dividers to both card edges while preserving the text and body positions. The final production export passes this geometry check at four widths in both themes. The sidebar uses Website, Genome Mapping and Protein Sequence for the corresponding existing routes. The Interfaces navigation index and compact contents picker share alphabetized display labels; the gallery retains its curated poster order. Next-study arrows occupy the gutter. Desktop documentation menus preserve pointer travel into their panels, while keyboard and Escape behavior remain available.

The mobile composition follows the specimen width, including narrow specimens beside the site's desktop rail. The frame contains independently scrolling content, pinned navigation and reachable actions. Buttons and form controls retain 44px targets. Live music meters use one decimal and a fixed value column so their updates do not move adjacent controls.

## Working applications

- AI chat keeps independent conversations and drafts, local response templates, saved replies and Markdown downloads. No model service is connected.
- Dashboard filters supplied production records by date and status, calculates the displayed metrics, and saves local review notes without changing invoice payment status.
- Social feed supports local publishing, contributor filters, likes, comments and following. Reaction controls have space around the post content. Team chat supports channels, messages, replies, pins and acknowledgements. Cancelling channel creation returns to the originating channel list, conversation or thread and preserves its reply draft and focus.
- Wallpaper generation uses a seeded geometric engine and exports a real PNG with a 6,144px long edge. Transit keeps planner, stop, saved-trip and profile state in one responsive app.
- The specialist studies cover culture observations, genomic region and sequence inspection, deliberately nonfunctional protein examples, robot debugging, circuit proposals, identity applications, patient visit preparation and a synthesized music session. Their host actions remain local and explicitly distinguish draft, requested and confirmed states. Patient visit notes and format preferences retain unsaved drafts when Back is used; saving remains an explicit action with a separate saved record.

The embedded About reference cards keep their descriptions and selection controls without repeating thumbnails below the selected artwork. The duplicate reference has been removed, and the personal credit reads Renn.

## Mobile OS

Each phone owns 16 core apps and its own records, settings, timers and playback state. Both phones appear side by side where space allows; compact specimens use native horizontal scroll snap without a study-level platform switch.

The iPhone keeps its Dynamic Island, status and safe areas, Home gesture, App Library, contextual back navigation, app toolbars and tabs, grouped Settings, Mailboxes hierarchy, and separate Control and Notification Centers. Its revised presentation uses flat paper surfaces, Vlak hairline icons, restrained fills and calmer type weights. Familiar Calendar, Clock, Photos and Safari faces remain distinct. Android retains its separate launcher, All Apps drawer, three-button system navigation, app bars, floating actions, note grid and Quick Settings shade.

These are browser interpretations using local example services. Their independent structures are informed by [Apple's tab-bar guidance](https://developer.apple.com/design/human-interface-guidelines/tab-bars), [iPhone app-switching gestures](https://support.apple.com/guide/iphone/switch-between-open-apps-iph1a1f981ad/ios), [Android app bars](https://developer.android.com/develop/ui/compose/components/app-bars), [Android scaffold composition](https://developer.android.com/develop/ui/compose/components/scaffold) and [Pixel navigation](https://support.google.com/pixelphone/answer/2781964). Calls, messages, mail, camera captures, weather and system switches do not operate external services. The Music app produces real local Web Audio tones. Its interface follows the actual AudioContext state: interruption updates both the app and system media controls to paused, a resume that leaves the context suspended stays resumable, and explicit playback can recover. Native touch checks distinguish vertical Home scrolling, a horizontal App Library gesture and horizontal navigation between the two phones.

## Documentation

The index and reading layout adapt [renatovaldes.com/essays](https://www.renatovaldes.com/essays/): a quiet metadata track, a 592px article measure and Inter throughout. The reader has no construction grid or grid setting. Its header breadcrumb and body share one content column; the body eyebrow aligns vertically with the first category label.

Reader settings reuse the same SettingsMark as the site chrome and compose canonical Vlak PopoverTitle, PopoverBody, Field, FieldLabel, ToggleGroup and Button components. Custom popover and control skins have been removed. Explicit Light and Dark use the exact Vlak token palettes, including control fills and borders; Auto inherits the surrounding site. Preferences remain local to the reader, and forced-colors mode retains system colors. Reading paragraphs and lists use Inter at weight 350. Reading controls have no hover fill; the outline keeps its proximity reveal. The article rail uses a compact heading outline informed by Paper and Google Docs. Short marks represent the document title and section hierarchy. Pointer approach or keyboard focus reveals their readable labels. Selecting a heading scrolls to it and pins the contents open; the Contents control can also pin it. Escape dismisses the outline and returns focus to its control. Mobile contents remain readable in a disclosure. Reduced motion removes the reveal transition. Appearance, text size and the index reading position remain local reader state.

## Music Player

The 23 requested recordings have verified titles, artists, albums, artwork, durations, direct store links and official iTunes preview URLs. Full song duration is separate from the approximately 30-second preview duration. The Vlak desktop layout has two panes: the library or queue beside the selected recording and transport. Mobile exposes Library, Playing and Queue as focused screens. Search, favorites, queue order, seeking, volume and mute operate on real state.

Every official preview starts through an explicit playback action and stops without automatically advancing to another preview. The selected recording carries the unmodified iTunes Store badge, a direct store link and courtesy attribution. Previews are promotional streams, not full-track downloads, offline caches or a continuous preview playlist. Users can attach their own files for full local playback, repeat and automatic advancement between attached tracks; those files are not uploaded. The source library and [Apple's Search API terms](https://developer.apple.com/library/archive/documentation/AudioVideo/Conceptual/iTuneSearchAPI/index.html) document this distinction. Badge artwork follows [Apple's identity guidance](https://marketing.services.apple/itunes-identity-guidelines).

The Sound view provides a live frequency spectrum and a five-band equalizer at 80 Hz, 250 Hz, 1 kHz, 4 kHz and 12 kHz, with ±12 dB gain, bypass and reset. The spectrum and level readout come from the playing audio's analyser. Local audio can use the processing graph directly. Selecting remote audio starts a bounded, uncached CORS HEAD probe. Play does not wait for that request: a pending or refused probe keeps the media element on direct playback and the interface identifies the unavailable processing controls. Confirmed-compatible media enters Web Audio; Play and AudioContext resume are requested in the same user action. A refused probe does not download the recording or substitute fabricated spectrum data. Reduced motion holds the visualizer still while playback and equalizer controls remain available.

The music session at `/interfaces/music` remains a separate sequencer: four synthesized tracks, clip and mix controls, and a real stereo wave export. Its mobile Session screen selects one track and presents its four clip launchers beside scene actions. Clip editing groups all 16 steps into four labeled beat rows instead of shrinking or horizontally panning the desktop grid. Clip and Mixer screens return focus to their launching control; transport remains pinned while their content scrolls.

## Maps and food provenance

Food browsing, menus, dish details, filters, bag and receipt remain separate focused phone screens. Tapping the already-active Bag action leaves the existing return path intact, so Back returns to the menu or browsing context in one step.

Mapbox GL JS renders San Francisco's Dogpatch neighborhood with vector layers recolored through Vlak tokens. Accessible vehicle markers and the toolbar share the map's selection and camera state. Mapbox Directions supplies street-following GeoJSON between sample stops using driving or cycling profiles. Failed route requests remove the previous route and expose retry. Positions, battery readings, arrival times and dispatch notes remain sample or local records.

The site reads `NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN` at build time; configuration is documented in `apps/www/.env.example`. Private configuration stays outside source control. Missing credentials and failed requests retain a labeled schematic fallback. Mapbox branding and attribution remain visible. The site CSP permits the SDK, stylesheet, API and worker according to [Mapbox's security guidance](https://docs.mapbox.com/mapbox-gl-js/guides/security-and-testing/). Mapbox and Three.js are site integrations, not component-package runtime dependencies.

Food ordering references these primary restaurant sources, checked on 7 September 2026:

| Restaurant | Location and scope | Source |
| --- | --- | --- |
| Joe's Kitchen | Berenkoog 35, Alkmaar. Caribbean, Antillean and Surinamese grill and rice dishes. | [Restaurant and menu](https://www.joeskitchenalkmaar.nl/) |
| Neder | Laat 85, Alkmaar. Seasonal Dutch ingredients, foraging and fermentation. | [Menu](https://www.nederrestaurant.nl/menu), [working approach](https://www.nederrestaurant.nl/werkwijze) |
| La Dune | Ruïnelaan 7, Bergen. French sourdough bakery, patisserie, market and natural wine. | [La Dune](https://ladunebergen.nl/) |
| Jacky's | Huiswaardersplein 1, Alkmaar. Trading as Jacky Alkmaar, with Oklahoma Smash and other burgers. | [Restaurant](https://jackyalkmaar.nl/), [May 2026 menu](https://jackyalkmaar.nl/wp-content/uploads/2026/05/Menukaart-Jacky-2026-DEF.pdf) |

Known prices are menu snapshots; La Dune's example dishes and prices are labeled samples. Delivery fees, timings, availability and checkout are illustrative. Local checkout creates a receipt without placing an order or charging a payment method.

The four restaurant images and 13 distinct dish images are generated illustrations, not photographs of the actual businesses or dishes. [Restaurant image prompts and provenance](assets/food-image-prompts-2026-09-07.json) and [dish image prompts and provenance](assets/food-dish-image-prompts-2026-09-07.json) record their sources, edits and final paths.

## Reactive scenes and licensed assets

The robot is an original articulated Three.js model. Shared forward kinematics drives the three joint pivots and tool readout. Draft targets appear as a ghost; confirming one joint preserves the current sampled values of the others. Playback speed, camera, trace, gripper, stepping and local camera-fault recovery affect the model. Reduced motion and a live vector fallback remain available.

The 3D Workspace and EV Controls share a geometry-only Evoque GLB: 204,453 triangles, 147,374 position/normal vertices, 66 meshes and 21 source materials in 4,819,464 bytes. Textures and UV data were removed without simplifying the mesh. Screen-space feature contours use LineSegments2 with a 1.05px width; a 0.009 reverse hull retains curved silhouettes. Paper-colored surfaces use polygon offset. Both renderers resolve the stable CSS token palette by compositing `--table-alt` over `--bg`, including repeated light/dark changes.

The 3D Workspace connects Fine lines, Ink lines, mesh, camera, zoom and turntable controls to the real renderer. Its [gallery preview manifest](assets/vehicle-render-preview-2026-09-07.json) records new light/dark captures from the stationary Fine lines camera, rendering settings, dimensions, hashes and the reproduction command.

EV's Vehicle mode shows the side profile. The latest Journey implementation uses a third-person perspective camera and an original looping monochrome 3D streetscape, with wheel movement driven by the simulation. It is illustrative geometry, not the mapped route to Utrecht. Energy separates an independently authored battery assembly in an isometric view. Three persistent reading blocks open detail panes; desktop panes occupy one third of the workspace and share the vehicle header row so their dividers align. Cabin, charging, connection, lock and playback controls update local state. Lights switch the site to dark mode and illuminate the ground; switching them off restores the preceding theme. The fresh production export passes EV mobile journeys and shared reading/detail checks, including the third-person Journey, aligned header and corrected rear trim. The shared vehicle also passes real mesh, camera, turntable, failure recovery and renderer cleanup checks.

The vehicle [model](https://sketchfab.com/3d-models/2022-land-rover-range-rover-evoque-034600db0cc94d64a7f3ccb19c7799fa) is by [tonielpro520](https://sketchfab.com/tonielpro520), licensed under [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/). Creator and license links sit below Components used on both study pages; [asset modifications](../apps/www/public/interfaces/concepts/vehicle-attribution.md) accompany the local GLB. The earlier `vehicle-line-v6.png` illustration remains an explicit rendering fallback.

Athena Labs replaces the Frontier brand and cube with a large, cropped head-and-shoulders bust drawn as fine contours and silhouettes. It turns and pans slowly, has no pause button, and stays still for reduced motion. [Bust of Athena](https://sketchfab.com/3d-models/bust-of-athena-6f372d03e69b48ee8901bdc6e48f17b5) by [yugengen](https://sketchfab.com/yugengen) is [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/); credits appear below Components used and [the adaptation manifest](../apps/www/public/interfaces/concepts/athena-license.json) records the source and geometry processing. The Website header uses the same paper, ink and hairlines as the other studies; its navigation underline meets the header divider, including the mobile disclosure layout.

Satellite operations uses a monochrome vector map with coastlines, pass corridors, targets and a narrow scanning reticle. Base, moisture and thermal views are distinct grayscale illustrations; pause and reduced motion stop the scanning overlays.

The icon family contains 154 drawn marks. Transportation adds `car`, `van` and `bicycle` and refines `truck`, using line and filled variants on the existing 16-unit grid and shared wheel baseline.

## Verification status

The fresh delivery export has been tested against the current application source. Results below distinguish passing product checks from the remaining release checks:

- Package build, typecheck, size budget and consumer tarball smoke passed, with 1,097 tests: 1,005 React, 60 core, 23 CLI and nine MCP. All 528 generated artifacts were byte-stable. Biome checked 793 files with zero errors, 73 warnings and two informational findings. The example check passed 167 isolated examples and 92 new previews.
- The main component browser suite passed 188 pages, including axe, the skip link, phone navigation and no sideways scroll at 390px.
- The fresh export passed all 192 layout/accessibility cases across 24 studies, four widths (320, 390, 1024 and 1440px) and both themes. The eight specialist studies also passed 64 layout/accessibility cases and complete local workflows at all four widths, including persisted drafts, exact records, JSON downloads and stereo wave export. A slow-tempo final-step release remained present through the full 18.5-second audio export.
- Mobile journeys passed for Agents, Wallpaper, Render, EV, Orbit and Website at 320, 390, 430 and a narrow 1024px layout. The gallery and all 24 study pages passed their shared navigation, build brief, clipboard failure fallback, source/component links and responsive checks. Shared chrome passed flush patient dividers, gutter arrows and stable music readouts. About passed lazy loading, reference navigation, keyboard focus, both themes and phone targets.
- Render controls passed at 320, 390, 1024 and 1440px with real geometry, Fine/Ink rendering, mesh, camera, touch, reduced motion and themes. The separate vehicle integration passed turntable movement, focus pause, responsive fit, asset failure/retry and renderer cleanup. These checks use the current EV/Render production export.
- Mobile OS's current full app helper passes on the export, including native touch scrolling, App Library versus phone-track gestures, both independent 16-app systems, actual oscillator creation, suspended-audio state, failed resume and successful recovery. The earlier source checkpoint additionally covered both platforms at 320, 390 and 1440px in both themes plus 18 home, Settings and Control Center accessibility checks.
- Documentation's canonical popover passed eight width/theme cases with exact canonical computed-style comparisons, 44px controls, 90–140% keyboard text-size limits, Escape/focus return and no overflow. Explicit reader palettes match Vlak tokens; local preferences leave the surrounding theme unchanged, Auto restores inheritance and forced colors use system colors. Full axe and scoped lint passed.
- Music Player's exported playback and Sound checkpoint passed eight width/theme cases, including accessibility, target sizes, the production CSP and a measured approximately +6 dB response to a +6 dB equalizer change. The delivery refresh runner reported one direct-audio timeout; the focused replacement regression passed all eight width/theme cases using trusted keyboard activation for pending and refused CORS probes. Each case requested immediate native playback without creating an AudioContext or media source. The original runner's nonzero exit remains recorded rather than relabeled as a clean full-run exit.

The dedicated Agents rerun passes, including its gallery link, after correcting directory redirects in the local test server. Inspiration's updated reference assertions pass all 15 checks. The main-branch media-control alignment changes have been merged and the final production build passed. All 528 generated outputs remain identical after a second generation pass.

The final export passes 64 first-eight crop geometry cases at 320, 390, 1024 and 1440px in both themes, without stylesheet injection: each title divider meets both crop edges, with its 20px text and vertical insets retained. No horizontal overflow or browser errors occurred. The focused post-merge media-control browser check passed all eight width/theme cases: control alignment, responsive wrapping, 44px targets, actual local playback, volume, mute and keyboard interaction, including the Music Player and EV transports.

Local evidence: `/tmp/vlak-delivery-browser-results.json`, `/tmp/vlak-delivery-e2e-interface-refresh.log`, `/tmp/vlak-direct-audio-regression-results.json`, `/tmp/vlak-doc-settings-palette-check.log` and `/tmp/vlak-specialist-crops-release-results.json`, `/tmp/vlak-delivery-e2e-inspiration-verified.log` and `/tmp/vlak-release-final-site-build.log`. Release validation is complete. No package publication or deployment result is claimed by these notes.
